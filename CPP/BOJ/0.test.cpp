int memorization[];
int N = INPUTDATA // 1~10만 까지 입력 가능
int wide = N //바닥 크기 초기화

int n = 1;
while ( memorization[n] < N ) {
 memoryzation[n] =+ memorizaiton[n-1];  // n!의 값 저장 
 n++;  
}

int maxNfactorial = n -1;
int result = 0;

for (int i = 0 ; i<= maxNfactorial; i++){

    if (result <  (wide - memorizaiton[i]) * (i+1){
    result = wide - memorizaiton[n]) * (i+1)
   }

}